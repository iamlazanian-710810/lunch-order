# 辦公室午餐訂餐系統 — 複製建立指南

> 用 Claude 全程協助建立，約半天完成，完全免費部署。

---

## 系統功能總覽

| 頁面 | 誰用 | 功能 |
|------|------|------|
| 午餐 `/` | 所有人 | **今日／明日** 兩個分頁，各自看店家與菜單圖 → 填餐點／價格／備註 → 送出 |
| 飲料/下午茶 `/drinks` | 所有人 | 同上，也分 **今日／明日** |
| 慶祝活動 `/celebration` | 所有人 | 依「活動」訂購（例：10月壽星慶生會），整段期間只算一張訂單，截止後鎖定 |
| 後台 `/admin` | 管理員（需 PIN） | 今日/明日店家菜單、四類訂單統計、慶祝活動管理、評價統計、員工管理 |
| 報表 `/report` | 所有人 | 月結每人費用、明細展開、自己餐點打星、匯出 Excel |

### 今日 / 明日 是怎麼分的

- 午餐與飲料/下午茶各有 **今日** 與 **明日預訂** 兩個分頁，共四個獨立欄位：
  今日午餐、明日午餐、今日飲料/下午茶、明日飲料/下午茶。
- 每一個欄位各有自己的店家名稱、菜單圖片、訂單與金額，互不干擾。
- 「明日」＝日曆上的明天。到了隔天，昨天預訂的那筆會自動變成「今日」的訂單，
  不需要任何手動搬移；月結報表也一律以「實際用餐日」計算。

### 慶祝活動是怎麼算的

- 管理員在後台建立活動，必填 **活動名稱**（這是什麼慶祝活動）與 **開放～截止日期**。
- 期間內同事隨時可以修改自己的訂單，整段期間**只算一張訂單，不分天**。
- 截止日之後，同事端自動鎖定變唯讀；管理員在後台仍可修正。
- 一次只會有一個活動在收單；舊活動保留在清單裡可以隨時回看。

---

## 後台功能細節

### 今日／明日設定
- 先選「今日」或「明日」，再分別設定午餐與飲料/下午茶的店家名稱與菜單圖片
- 上傳菜單圖片（手機直接拍照），上傳時自動清除 90 天前的舊圖
- 訂單統計可切換四個獨立類別（今日午餐／今日飲料/下午茶／明日午餐／明日飲料/下午茶），
  按鈕上直接顯示各類目前有幾筆
- 每一類都能用 **依員工** 或 **依品項** 兩種視角看，並一鍵複製叫餐清單

### 慶祝活動
- 新增／編輯／刪除活動（活動名稱、開放與截止日期、店家、補充說明、菜單圖片）
- 清單顯示每個活動的狀態：尚未開放／開放訂購中／已截止
- 點選活動可看該活動的完整訂單彙總與合計，一鍵複製叫餐清單
- 刪除活動會連同該活動的訂單一起刪除，刪除前會先告訴你有幾筆

### 評價統計
- 依「店家／活動」與「餐點」統計平均星數排行

### 員工管理
- 新增／刪除員工名單
- **刪除員工會連同他所有訂餐紀錄一起永久刪除**，月結金額會跟著變少；刪除前會先提示有幾筆

---

## 技術架構

| 項目 | 選擇 | 費用 |
|------|------|------|
| 前後端 | Next.js 16（App Router） | 免費 |
| 資料庫 | Supabase（PostgreSQL） | 免費方案 |
| 部署 | Vercel | 免費方案 |
| 身分識別 | 下拉選名字（無帳密） | — |
| 後台保護 | PIN 碼（環境變數設定） | — |

**容量說明：** 點餐文字資料幾乎不佔空間；菜單圖片每張壓縮後約 200–300 KB，上傳時自動清除 90 天前的舊圖，免費方案（500 MB）可用數年。

---

## 開始前請備妥

- [ ] GitHub 帳號並登入瀏覽器
- [ ] Supabase 帳號（用 GitHub 登入即可）
- [ ] Vercel 帳號（用 GitHub 登入即可）
- [ ] 想好後台 PIN 碼（預設 `1234`）
- [ ] 10 分鐘陪 Claude 操作（需要時會請你按確認）

---

## 請 Claude 幫你建立（複製以下 Prompt）

```
幫我建立一個辦公室午餐訂餐系統，需要全程幫我操作，我不懂技術。

【系統需求】
- 公司約 10 人，每天管理員上傳菜單圖片，員工手動填寫點餐
- 公司先代墊，月底統計每人費用再收款
- 純網頁、免費部署、不需安裝任何東西

【技術規格】
- 前後端：Next.js（App Router）
- 資料庫：Supabase（免費方案）
- 部署：Vercel（免費方案）
- 身分識別：下拉選名字，不需帳號密碼
- 後台保護：PIN 碼（NEXT_PUBLIC_ADMIN_PIN 環境變數）

【頁面規格】

1. 主頁 /
   - 顯示管理員上傳的今日菜單圖片（可點圖全螢幕放大）
   - 下拉選「我是誰」
   - 手動填寫點餐，每筆有：餐點名稱、價格、備註，可新增多筆
   - 送出後右側顯示今日所有人訂單統計（含備註獨立顯示）
   - 員工只能刪除自己送出的訂單

2. 後台 /admin（進入需輸入 PIN 碼，驗證後本次瀏覽器 session 有效）
   - 今日設定 Tab：
     * 上傳今日菜單圖片（手機可直接拍照），上傳時自動清除 90 天前的舊圖
     * 即時訂單列表，支援「依員工」和「依品項」兩種視角切換
     * 依品項視角：同餐點合併顯示，備註不同則分開列，顯示誰點、幾份、小計
     * 管理員可刪除任意人的訂單
     * 一鍵複製叫餐清單
   - 本月記錄 Tab：依日期分組，每天展開所有訂單明細，可刪除任意筆
   - 餐廳管理 Tab：新增／刪除餐廳與菜單（供參考用）
   - 員工管理 Tab：新增／刪除員工名單

3. 報表 /report
   - 選年月 → 每人費用總表 → 可展開每日明細（含備註）→ 匯出 CSV

【資料庫結構】
- employees（id, name）
- restaurants（id, name, phone, note, sort_order）
- menu_items（id, restaurant_id, name, price）
- daily_schedule（id, date UNIQUE, restaurant_id, menu_image TEXT）
- orders（id, date, employee_id, menu_item_id 可空, item_name TEXT, note TEXT, qty, subtotal）
- 所有 Table 開啟 RLS，policy 為 allow all
- daily_schedule.restaurant_id FK 設定 ON DELETE SET NULL

【請幫我做的事】
1. 建立 Next.js 專案並寫好所有程式碼
2. 建立 Supabase 專案並執行 schema SQL
3. 建立 GitHub repo 並推上去
4. 部署到 Vercel 並設定所有環境變數（含 NEXT_PUBLIC_ADMIN_PIN）
5. 全程幫我操作，需要授權時再告訴我
```

---

## 日常使用流程

```
每天早上（管理員）
  → /admin 輸入 PIN → 今日／明日設定 → 選「今日」→ 上傳午餐菜單照片
  → 若明天的店家已經確定，切到「明日」一起設定好

中午前（所有人）
  → 午餐頁 → 選「今日」或「明日預訂」→ 選自己名字
  → 看菜單圖（可點放大）→ 填餐點/價格/備註 → 送出

訂餐截止後（管理員）
  → 今日／明日設定 → 訂單統計選「今日午餐」→ 切換「依品項」
  → 複製叫餐清單 → 打電話訂餐

有慶祝活動時（管理員）
  → /admin → 慶祝活動 → 填活動名稱與開放～截止日期 → 建立活動
  → 截止後 → 點該活動 → 複製叫餐清單（整段期間彙整成一張表）

月底（管理員）
  → /report → 選月份 → 對帳 → 匯出 Excel 收款
```

---

## 修改 PIN 碼

在 Vercel 後台修改環境變數 `NEXT_PUBLIC_ADMIN_PIN`，重新部署即生效。

---

## 給第一次讓 Claude 建系統的建議

1. **一次說清楚需求** — 中途改功能會大幅增加時間，建議先把所有功能確認好再開始
2. **提前登入所有帳號** — GitHub、Supabase、Vercel 都在瀏覽器登入好
3. **Claude 請你確認時快點回應** — 它在等你授權，等太久可能 timeout
4. **部署後再測試** — 以線上版本為準
5. **讓 Claude 先問你問題** — 開頭告訴 Claude「先列出你需要知道的資訊，問完再開始做」，可以節省大量來回
